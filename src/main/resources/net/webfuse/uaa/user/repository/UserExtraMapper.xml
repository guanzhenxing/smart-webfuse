<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="net.webfuse.uaa.user.repository.UserExtraMapper">
  <resultMap id="BaseResultMap" type="net.webfuse.uaa.user.domain.UserExtra">
    <id column="USER_ID" jdbcType="VARCHAR" property="userId" />
    <result column="ID" jdbcType="BIGINT" property="id" />
  </resultMap>
  <sql id="Example_Where_Clause">
    <where>
      <foreach collection="oredCriteria" item="criteria" separator="or">
        <if test="criteria.valid">
          <trim prefix="(" prefixOverrides="and" suffix=")">
            <foreach collection="criteria.criteria" item="criterion">
              <choose>
                <when test="criterion.noValue">
                  and ${criterion.condition}
                </when>
                <when test="criterion.singleValue">
                  and ${criterion.condition} #{criterion.value}
                </when>
                <when test="criterion.betweenValue">
                  and ${criterion.condition} #{criterion.value} and #{criterion.secondValue}
                </when>
                <when test="criterion.listValue">
                  and ${criterion.condition}
                  <foreach close=")" collection="criterion.value" item="listItem" open="(" separator=",">
                    #{listItem}
                  </foreach>
                </when>
              </choose>
            </foreach>
          </trim>
        </if>
      </foreach>
    </where>
  </sql>
  <sql id="Base_Column_List">
    USER_ID, ID
  </sql>
  <select id="selectByExample" parameterType="net.webfuse.uaa.user.domain.UserExtraCriteria" resultMap="BaseResultMap">
    select
    <if test="distinct">
      distinct
    </if>
    'false' as QUERYID,
    <include refid="Base_Column_List" />
    from USER_EXTRA
    <if test="_parameter != null">
      <include refid="Example_Where_Clause" />
    </if>
    <if test="orderByClause != null">
      order by ${orderByClause}
    </if>
  </select>
  <select id="selectByPrimaryKey" parameterType="java.lang.String" resultMap="BaseResultMap">
    select 
    <include refid="Base_Column_List" />
    from USER_EXTRA
    where USER_ID = #{userId,jdbcType=VARCHAR}
  </select>
  <delete id="deleteByPrimaryKey" parameterType="java.lang.String">
    delete from USER_EXTRA
    where USER_ID = #{userId,jdbcType=VARCHAR}
  </delete>
  <insert id="insert" parameterType="net.webfuse.uaa.user.domain.UserExtra">
    <selectKey keyProperty="id" order="BEFORE" resultType="java.lang.Long">
      CALL IDENTITY()
    </selectKey>
    insert into USER_EXTRA (USER_ID, ID)
    values (#{userId,jdbcType=VARCHAR}, #{id,jdbcType=BIGINT})
  </insert>
  <insert id="insertSelective" parameterType="net.webfuse.uaa.user.domain.UserExtra">
    <selectKey keyProperty="id" order="BEFORE" resultType="java.lang.Long">
      CALL IDENTITY()
    </selectKey>
    insert into USER_EXTRA
    <trim prefix="(" suffix=")" suffixOverrides=",">
      <if test="userId != null">
        USER_ID,
      </if>
      ID,
    </trim>
    <trim prefix="values (" suffix=")" suffixOverrides=",">
      <if test="userId != null">
        #{userId,jdbcType=VARCHAR},
      </if>
      #{id,jdbcType=BIGINT},
    </trim>
  </insert>
  <select id="countByExample" parameterType="net.webfuse.uaa.user.domain.UserExtraCriteria" resultType="java.lang.Long">
    select count(*) from USER_EXTRA
    <if test="_parameter != null">
      <include refid="Example_Where_Clause" />
    </if>
  </select>
  <update id="updateByPrimaryKeySelective" parameterType="net.webfuse.uaa.user.domain.UserExtra">
    update USER_EXTRA
    <set>
      <if test="id != null">
        ID = #{id,jdbcType=BIGINT},
      </if>
    </set>
    where USER_ID = #{userId,jdbcType=VARCHAR}
  </update>
  <update id="updateByPrimaryKey" parameterType="net.webfuse.uaa.user.domain.UserExtra">
    update USER_EXTRA
    set ID = #{id,jdbcType=BIGINT}
    where USER_ID = #{userId,jdbcType=VARCHAR}
  </update>
</mapper>