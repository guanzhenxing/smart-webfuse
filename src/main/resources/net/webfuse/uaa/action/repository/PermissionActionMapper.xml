<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="net.webfuse.uaa.action.repository.PermissionActionMapper">
  <resultMap id="BaseResultMap" type="net.webfuse.uaa.action.domain.PermissionAction">
    <result column="ID" jdbcType="BIGINT" property="id" />
    <result column="PERMISSION_ID" jdbcType="BIGINT" property="permissionId" />
    <result column="ACTION_ID" jdbcType="BIGINT" property="actionId" />
  </resultMap>
  <sql id="Example_Where_Clause">
    <where>
      <foreach collection="oredCriteria" item="criteria" separator="or">
        <if test="criteria.valid">
          <trim prefix="(" prefixOverrides="and" suffix=")">
            <foreach collection="criteria.criteria" item="criterion">
              <choose>
                <when test="criterion.noValue">
                  and ${criterion.condition}
                </when>
                <when test="criterion.singleValue">
                  and ${criterion.condition} #{criterion.value}
                </when>
                <when test="criterion.betweenValue">
                  and ${criterion.condition} #{criterion.value} and #{criterion.secondValue}
                </when>
                <when test="criterion.listValue">
                  and ${criterion.condition}
                  <foreach close=")" collection="criterion.value" item="listItem" open="(" separator=",">
                    #{listItem}
                  </foreach>
                </when>
              </choose>
            </foreach>
          </trim>
        </if>
      </foreach>
    </where>
  </sql>
  <sql id="Base_Column_List">
    ID, PERMISSION_ID, ACTION_ID
  </sql>
  <select id="selectByExample" parameterType="net.webfuse.uaa.action.domain.PermissionActionCriteria" resultMap="BaseResultMap">
    select
    <if test="distinct">
      distinct
    </if>
    'false' as QUERYID,
    <include refid="Base_Column_List" />
    from PERMISSION_ACTION
    <if test="_parameter != null">
      <include refid="Example_Where_Clause" />
    </if>
    <if test="orderByClause != null">
      order by ${orderByClause}
    </if>
  </select>
  <insert id="insert" parameterType="net.webfuse.uaa.action.domain.PermissionAction">
    <selectKey keyProperty="id" order="BEFORE" resultType="java.lang.Long">
      CALL IDENTITY()
    </selectKey>
    insert into PERMISSION_ACTION (ID, PERMISSION_ID, ACTION_ID
      )
    values (#{id,jdbcType=BIGINT}, #{permissionId,jdbcType=BIGINT}, #{actionId,jdbcType=BIGINT}
      )
  </insert>
  <insert id="insertSelective" parameterType="net.webfuse.uaa.action.domain.PermissionAction">
    <selectKey keyProperty="id" order="BEFORE" resultType="java.lang.Long">
      CALL IDENTITY()
    </selectKey>
    insert into PERMISSION_ACTION
    <trim prefix="(" suffix=")" suffixOverrides=",">
      ID,
      <if test="permissionId != null">
        PERMISSION_ID,
      </if>
      <if test="actionId != null">
        ACTION_ID,
      </if>
    </trim>
    <trim prefix="values (" suffix=")" suffixOverrides=",">
      #{id,jdbcType=BIGINT},
      <if test="permissionId != null">
        #{permissionId,jdbcType=BIGINT},
      </if>
      <if test="actionId != null">
        #{actionId,jdbcType=BIGINT},
      </if>
    </trim>
  </insert>
  <select id="countByExample" parameterType="net.webfuse.uaa.action.domain.PermissionActionCriteria" resultType="java.lang.Long">
    select count(*) from PERMISSION_ACTION
    <if test="_parameter != null">
      <include refid="Example_Where_Clause" />
    </if>
  </select>
</mapper>